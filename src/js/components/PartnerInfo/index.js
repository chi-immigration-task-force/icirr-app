import PartnerInfo from './PartnerInfo';

/**
 * This is, perhaps, overly fancy. Just makes it less likely that someone
 * will try to import the private / specific PartnerInfoRightSide component.
 */
export default PartnerInfo;
